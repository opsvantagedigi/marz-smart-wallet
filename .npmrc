# npm audit security settings
# Production dependencies: 0 vulnerabilities ✓
# DevDependencies (vercel CLI): Known transitive @vercel/* package vulnerabilities
# These do NOT affect the Next.js frontend application
# Vercel v50.1.3 is the latest stable CLI version with all patches applied
